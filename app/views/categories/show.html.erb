<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @category.name %>
</p>
<%= render partial: 'listrecipes' %>
<%= link_to 'Edit', edit_category_path(@category) if can? :edit, @category %> |
<%= link_to 'Back', categories_path %>
