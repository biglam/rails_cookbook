<%= form_for(@recipe) do |f| %>
  <% if @recipe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@recipe.errors.count, "error") %> prohibited this recipe from being saved:</h2>

      <ul>
      <% @recipe.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :instructions %><br>
    <%= f.text_area :instructions %>
  </div>
  <div class="field">
    <%= f.label :category_id %><br>
    <%= f.collection_select :category_id, Category.order(:id),:id,:name, include_blank: false %>
  </div>
  <div class="field">
  <p>New ingredients must be <%= link_to 'created', new_ingredient_path %> before they can be selected</p>
  <%#= f.collection_select :ingredients, Ingredient.order(:name), :id, :name, include_blank: false %>
  <%#ß= collection_check_boxes(:recipe, :ingredients, Ingredients.all, :id, :name %>
  <%= f.collection_check_boxes(:ingredient_ids, Ingredient.order(:name), :id, :name) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
